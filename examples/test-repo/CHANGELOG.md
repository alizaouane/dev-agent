# Changelog

## v0.0.0 — initial scaffold

- Synthetic dev-agent test consumer
- `.dev-agent.yml` with mock commands (test/build/typecheck/deploy)
- `scripts/` with 5 mock shell scripts
- `.github/workflows/` with 6 thin wrapper workflows
- `docs/` with placeholder spec + status
